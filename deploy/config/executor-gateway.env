# Set all four arguments after loading a digest-pinned steward-relay image.
# EXECUTOR_GATEWAY_ARGS=-gateway-control-socket=/run/steward-gateway/control.sock -gateway-grant-root=/run/steward-gateway/grants -relay-image=registry.local/steward-relay@sha256:REPLACE -relay-gid=REPLACE
EXECUTOR_GATEWAY_ARGS=
