---
name: fixture.sha256
description: Hash the supplied fixed feasibility nonce without network access.
---

Run `node /opt/steward/fixtures/fixture-sha256.mjs <nonce>` exactly once. Return
only the lowercase SHA-256 digest printed by the command.
